/**
 * Справочник направляющих профилей для MVP.
 *
 * ВНИМАНИЕ: геометрические характеристики являются оценочными и рассчитаны
 * для условного Г-образного (уголкового) тонколистового профиля из
 * оцинкованной стали с размерами 40×40×1.2 мм. Для проектирования
 * необходимо использовать сертифицированные данные производителя
 * (ТУ, паспорт системы).
 */

import type { Profile } from "../calc/types";
import { STEEL_DEFAULT } from "../calc/steel";

/**
 * Геометрические характеристики тонкостенного Г-образного (равнополочного)
 * профиля 40×40×1.2 мм относительно геометрической оси X-X (параллельной
 * одной из полок). Расчёт выполнен по первым принципам (интегрирование
 * прямоугольных элементов), не для главных осей инерции.
 *
 * Площадь: A = 40·1.2 + (40 − 1.2)·1.2 = 94.56 мм².
 * Центр тяжести от полки, параллельной оси x: y_c ≈ 10.45 мм.
 * Ix ≈ 15 295 мм⁴ (1.53 см⁴).
 * y_max (наиболее удалённая от центра тяжести фибра) ≈ 29.55 мм.
 * Wx_min ≈ Ix / y_max ≈ 518 мм³ (0.52 см³).
 *
 * Это сечение — геометрическое (W_x), без учёта редукции тонкостенной
 * полки по СП 260.1325800 (Cold-formed). Для MVP редукцию не применяем;
 * это помечено как предупреждение в расчётном модуле.
 */
export const PROFILE_G_40x40x1_2_ZN: Profile = {
  id: "g_40x40x1.2_zn",
  name: "Г-образный профиль 40×40×1.2 мм, оцинкованная сталь",
  manufacturer: "Металл Профиль (условные характеристики)",
  shape: "L",
  sizeMm: { width: 40, height: 40, thickness: 1.2 },
  steel: STEEL_DEFAULT,
  section: {
    area: 94.56,
    ix: 15_295,
    wx: 518,
    radiusX: Math.sqrt(15_295 / 94.56),
  },
  note:
    "Характеристики рассчитаны по первым принципам для геометрической оси X-X. " +
    "Редукция тонкостенной полки (СП 260.1325800) не учтена. " +
    "Для проектирования использовать паспорт системы Металл Профиль.",
};

export const PROFILES: Profile[] = [PROFILE_G_40x40x1_2_ZN];

export function getProfileById(id: string): Profile | undefined {
  return PROFILES.find((p) => p.id === id);
}
